\section{Frozen Transfer to Public Speech Benchmarks}
\label{sec:transfer}
\label{sec:transfer-neg}

The out-of-distribution test the type-shortcut audit demands: we take
the deployed gate \emph{frozen} --- weights, features, and layer choice
fit once on the internal calibration mix --- and run the complete live
loop of \S\ref{sec:live} on six public speech benchmarks: Speech
TriviaQA, Speech Web Questions, Llama Questions, and Reasoning QA from
OpenAudioBench \citep{openaudiobench2025} (spoken renderings of
\citealp{joshi2017triviaqa}, \citealp{berant2013webquestions}, and the
set of \citealp{nachmani2024spoken}), SD-QA (real dialectal human
speech) \citep{faisal2021sdqa}, and VoiceBench AlpacaEval
\citep{chen2024voicebench,taori2023alpaca}. All audio is the
benchmarks' official pre-rendered audio; no query ever touches probe
calibration. The only per-pool adaptation is \emph{label-free}: tier
thresholds are quantiles of the probe's own score distribution on each
pool, consuming no labels and no judge calls. It buys budget control,
not discrimination: one absolute threshold frozen on the internal pool
still beats matched-rate random on four of five pools, but fires at
2--48\% across them (Appendix~\ref{app:fixedthr}). Where an official
protocol exists we score with it (the OpenAudioBench judge;
VoiceBench's 1--5 judge), and our chat-mode control reproduces the
official anchors on-scale.

\begin{table}[t]
\centering\scriptsize
\setlength{\tabcolsep}{3.5pt}
\renewcommand{\arraystretch}{0.92}
\caption{Main transfer results: methods $\times$ five public speech
QA benchmarks (TriviaQA/WebQ/Llama Q.\ OAB official judge, $n$ = 250;
SD-QA/Reasoning zh reference-anchored, $n$ = 200/202); heard accuracy
in \%, $\pm$2--3 point replication floor. Bottom: second duplex family
(NemotronLabs-VoiceChat-11B, same frozen recipe, offline re-mix,
official judges, Appendix~\ref{app:nvda}); English-only, so its avg
spans four benchmarks over its own floor, and its gate clears
matched-rate random on every pool at every budget (per-budget random
references and permutation tests: Table~\ref{tab:nvda-remix}).
Gold-inject view at matched budgets: Table~\ref{tab:oracle}. $\Delta$ = avg gain over always-local
(points). AlpacaEval (1--5): 3.99$\to$4.35 gated, 4.76 always-escalate,
no gain over random on MiniCPM; the second family does gain
(3.55$\to$4.46 vs.\ random 4.23).}
\label{tab:transfer}
\begin{tabular}{lccccccc}
\toprule
& TriviaQA & WebQ & Llama Q. & SD-QA & Reason.\ zh & avg & $\Delta$ \\
\midrule
\multicolumn{8}{l}{\emph{live heard view (deployed loop)}} \\
always-local          & 66.4 & 57.2 & 84.0 & 51.5 & 58.9 & 63.6 & --- \\
\;+ gate @15\%        & 73.2 & 62.8 & 90.4 & 60.0 & 65.3 & 70.3 & +6.7 \\
\;+ gate @30\%        & 80.0 & 68.0 & 90.4 & 72.0 & 71.3 & 76.3 & +12.7 \\
\;\textbf{+ gate @50\%} & \textbf{86.0} & \textbf{73.2} & \textbf{94.8} & \textbf{79.5} & \textbf{77.2} & \textbf{82.1} & \textbf{+18.5} \\
always-escalate (measured) & 88.0 & 80.8 & 93.6 & 88.5 & 83.7 & 86.9 & +23.3 \\
always-expert (gold-text ceiling) & 96.8 & 86.4 & 92.8 & 93.0 & 87.1 & 91.2 & +27.6 \\
\midrule
\multicolumn{8}{l}{\emph{second family: NemotronLabs-VoiceChat-11B (offline re-mix, official judges)}} \\
always-local          & 35.6 & 37.6 & 70.5 & 31.0 & --- & 43.7 & --- \\
\;+ gate @15\%        & 49.8 & 48.0 & 78.6 & 42.5 & --- & 54.7 & +11.0 \\
\;+ gate @30\%        & 61.5 & 58.8 & 83.8 & 54.5 & --- & 64.6 & +21.0 \\
\;\textbf{+ gate @50\%} & \textbf{74.1} & \textbf{69.6} & \textbf{87.6} & \textbf{67.5} & --- & \textbf{74.7} & \textbf{+31.0} \\
\;matched random @50\% & 65.6 & 60.2 & 81.8 & 60.0 & --- & 66.9 & +23.2 \\
always-expert (ceiling) & 95.5 & 82.8 & 93.2 & 89.0 & --- & 90.1 & +46.4 \\
\midrule
probe AUC (frozen)    & 0.789 & 0.785 & 0.806 & 0.792 & 0.683 & 0.771 & --- \\
probe AUC (2nd family) & 0.775 & 0.771 & 0.720 & 0.754 & --- & 0.755 & --- \\
official offline      & 75.5 & 70.2 & ---  & ---  & ---  & ---  & --- \\
\bottomrule
\end{tabular}
\end{table}

\textbf{The frozen gate improves all five QA benchmarks}
(Table~\ref{tab:transfer}), monotonically in budget.
On Speech TriviaQA the 9B system reaches \textbf{86.0\% live}
--- against its own official offline 75.5\% and 62.9\% for Qwen3-Omni-30B
\citep{qwen3omni2025} at $3.3\times$ the parameters: selective cloud
escalation outperforms larger standalone backbones. The cleanest
evidence that the
signal is difficulty rather than surface is cross-lingual: an
English-only calibration expansion moved \emph{Chinese} Reasoning QA
from AUC 0.621 to 0.683, the largest per-pool gain. On Llama Questions
--- single-type, so no shortcut to exploit --- \textbf{selective
escalation matches the expert at half the expert calls}: the probe's
split is decisive (kept-local 97.6\% vs.\ escalated 69.6\%, $z{=}6.46$),
the expert's whole advantage lands where the probe sends it
(69.6\%$\to$88.8\%, $p{<}.0001$), and a measured live always-escalate
arm lands \emph{below} selective (93.6 vs.\ 94.8 at half the calls); matched-rate precision reaches 95\% of the
base-rate cap (Appendix~\ref{app:transferdetail}).

Two honest negatives: \textbf{AlpacaEval} has no ``missing fact''
event to read, and \textbf{Web Questions and SD-QA are flat} (strict
alias labels; the failures the gate ranks highest are ones the expert
misses too). Neither is intrinsic --- on the second duplex family the
same recipe beats matched-rate random on \emph{all five} pools,
AlpacaEval included ($p\le.0004$), so the flat pools are a headroom
property of the pool $\times$ backbone pairing
(Appendices~\ref{app:nvda},~\ref{app:transferdetail}).
